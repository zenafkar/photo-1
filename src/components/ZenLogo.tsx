

export const ZenLogo = ({ className = "h-8 w-auto" }: { className?: string }) => (
  <div className={`flex items-center justify-center ${className}`}>
    <img src="/logo-baru.png" alt="ZenStudio Logo" className="h-full w-auto object-contain" />
  </div>
);
